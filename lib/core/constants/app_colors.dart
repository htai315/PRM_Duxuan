import 'package:flutter/material.dart';

/// Bảng màu Du Xuân — Tết Nguyên Đán ấm áp
class AppColors {
  AppColors._();

  // ─── Đỏ Tết (Primary) ─────────────────────────────────
  static const Color primary = Color(0xFFD4403A);
  static const Color primarySoft = Color(0xFFE87C6F);
  static const Color primaryDeep = Color(0xFFA82828);

  // ─── Vàng Mai (Accent) ─────────────────────────────────
  static const Color gold = Color(0xFFE8A838);
  static const Color goldLight = Color(0xFFF5D090);
  static const Color goldDeep = Color(0xFFC08520);

  // ─── Hồng Đào (Blossom) ────────────────────────────────
  static const Color blossom = Color(0xFFF8A4B8);
  static const Color blossomLight = Color(0xFFFDD5DF);
  static const Color blossomDeep = Color(0xFFE06888);

  // ─── Nền ấm ───────────────────────────────────────────
  static const Color bgWarm = Color(0xFFFFF5EC);
  static const Color bgPeach = Color(0xFFFFE8D6);
  static const Color bgCream = Color(0xFFFFFBF5);

  // ─── Text ──────────────────────────────────────────────
  static const Color textDark = Color(0xFF3D2415);
  static const Color textMedium = Color(0xFF7A5C4F);
  static const Color textLight = Color(0xFFB39888);

  // ─── Trắng / Surface ──────────────────────────────────
  static const Color white = Color(0xFFFFFFFF);
  static const Color whiteSoft = Color(0xFFFFFAF5);
  static const Color divider = Color(0xFFEBDDD2);

  // ─── Status ────────────────────────────────────────────
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD4403A);
}
